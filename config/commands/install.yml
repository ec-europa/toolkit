command:
  toolkit:
    install-clean:
      options:
        config-file: ${toolkit.clean.config_file}
    install-clone:
      options:
        dumpfile: ${toolkit.clone.dumpfile}
        config-file: ${toolkit.clone.config_file}
