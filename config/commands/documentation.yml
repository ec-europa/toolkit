command:
  toolkit:
    generate-documentation:
      options:
        token: ${env.GITHUB_API_TOKEN}
        repo: 'https://%s@github.com/ec-europa/toolkit'
        docs-dir: docs
        tmp-dir: docs_tmp
        branch: documentation
