command:
  toolkit:
    install-dump:
      options:
        dumpfile: ${toolkit.clone.dumpfile}
        myloader: false
    create-dump:
      options:
        dumpfile: ${toolkit.clone.dumpfile}
  moodle:
    install-dump:
      options:
        dumpfile: ${toolkit.clone.dumpfile}
