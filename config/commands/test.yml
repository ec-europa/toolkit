command:
  toolkit:
    test-behat:
      options:
        from: ${toolkit.test.behat.from}
        to: ${toolkit.test.behat.to}
    test-phpunit:
      options:
        execution: ${toolkit.test.phpunit.execution}
        from: ${toolkit.test.phpunit.from}
        to: ${toolkit.test.phpunit.to}
    setup-eslint:
      options:
        config: ${toolkit.lint.yaml.config}
        ignores: ${toolkit.lint.yaml.ignores}
        drupal-root: ${drupal.root}
        packages: ${toolkit.lint.yaml.packages}
    lint-yaml:
      options:
        config: ${toolkit.lint.yaml.config}
        extensions: ${toolkit.lint.yaml.extensions}
