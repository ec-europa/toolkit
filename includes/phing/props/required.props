# ------------------------------------------------------------------------------
# These are the minimal required properties that need to be in the project props
# file to perform a build on CI. To make sure that developers have this
# information validation is implemented to check if all properties have been
# defined.
# ------------------------------------------------------------------------------

# Subsite configuration.
# ----------------------
project.id = myproject
project.install.modules = myproject_core
project.name = My Project
project.url.production =

# Solr configuration.
# -------------------
solr.type = d7_apachesolr

# Admin configuration.
# --------------------
project.admin.email = ${admin.username}@example.com

# Platform configuration.
# -----------------------
profile = multisite_drupal_standard
platform.package.version = 2.5


