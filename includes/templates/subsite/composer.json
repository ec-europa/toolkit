{
    "name": "ec-europa/subsite",
    "require": {
        "ec-europa/ssk": "dev-master as 3.0.0"
    },
    "scripts": {
        "post-install-cmd": "@ssk-install",
        "post-update-cmd": "@ssk-install",
        "ssk-install": "PROJECT=$(pwd) composer run-script ssk-install -d ./vendor/ec-europa/ssk"
    }
}
