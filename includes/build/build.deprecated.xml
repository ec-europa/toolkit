<?xml version="1.0" encoding="UTF-8" ?>

<project name="My subsite" default="help">

    <!-- Target build-dev has been replaced by build-code. -->
    <target name="build-dev" hidden="true">
        <deprecated target="build-code" />
    </target>

    <!-- Target rebuild-dev has been replaced by build-keep. -->
    <target name="rebuild-dev" hidden="true">
        <deprecated target="build-keep" />
    </target>

</project>
